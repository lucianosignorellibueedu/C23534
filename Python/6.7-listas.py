# pop([índice]): Elimina y devuelve el elemento en la posición especificada (o el último elemento si no se proporciona un índice).
numeros = [1, 2, 3, 4, 5]
elemento_eliminado = numeros.pop(2)
# numeros ahora es [1, 2, 4, 5], elemento_eliminado es 3
